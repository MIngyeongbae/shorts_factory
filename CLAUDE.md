# shorts-factory

AI 파이프라인으로 지식 쇼츠(9:16, 60초 안팎 — ADR-0057)를 자동 생성하는 프로젝트. **특정 채널의 스타일을 복제하지 않는다** (ADR-0050) — 레퍼런스 채널 실측은 분량·전환 값의 출처로만 남는다.

**첫 원칙: 청자는 지식이 없고 관심도 없다** (ADR-0047). 과제는 "많이 담기"가 아니라 흥미를 유발하고 질 좋은 정보를 1분에 효과적으로 전달하는 것이다. 숫자는 정확해야 하지만 전체 글에서 제 역할을 해야 하고, **이미지는 그 전달을 보조하는 수단이다**.

**소재 도메인에 제한이 없다** (ADR-0033이 ADR-0015를 폐기). 판별 기준은 **그림이 이해를 보조할 수 있는가** 하나다 (ADR-0047이 "설명의 일부를 질 수 있는가"에서 낮췄다) — 기준은 도메인이 아니라 **매체 적합성**이고, 분위기용 삽화만 나오는 소재는 여전히 탈락이다. 근거는 1차 사료 필수가 아니며 위키백과도 인정한다 (ADR-0016). 확정되지 않은 유력 학설은 `confidence: medium`으로 싣고 대본에서 추정 표현으로 쓴다.

**대본의 틀은 소재마다 설계한다** (ADR-0033). 고정된 단 구성은 폐기됐다 — 틀이 소재를 고르는 순서를 뒤집는다. 남는 제약은 구조가 아니라 결과다 (분량 엔벨로프 + 훅이 앞 + 수미상관).

## 절대 원칙 (스펙 우선)

1. **스펙이 코드보다 우선한다.** 모든 구현/수정 전에 `specs/`의 관련 스펙을 먼저 읽는다. 코드와 스펙이 충돌하면 스펙이 정답이고, 스펙이 틀렸다고 판단되면 코드를 고치지 말고 먼저 스펙 수정을 제안한다.
2. **아키텍처 결정은 ADR로 남긴다.** 라이브러리 선택, 파이프라인 단계 변경, 데이터 계약 변경 등 되돌리기 비싼 결정은 `docs/adr/`에 기록 후 구현한다. `/adr` 커맨드 사용.
3. **연출은 닫힌 어휘에서 고른다.** 구도·전환·카메라는 `[3s] scenetable`이 씬마다 고르되 (ADR-0049) **자유 기술이 아니라** `specs/schema/vocab.json`의 **어휘에서 선택**한다 (ADR-0033이 ADR-0001을 폐기). 스타일 문자열·`camera` enum·클립 기하 0.6초는 자유화 대상이 **아니다**. 어휘에 없는 값이 필요하면 어휘를 추가하는 PR을 먼저 만든다.
4. **단계 간 계약은 JSON 스키마다.** 파이프라인 각 단계의 입출력은 `specs/05-pipeline.md`가 정의하고, 기계가 읽는 정본은 `specs/schema/`에 있다. **코드는 상수를 선언하지 않고 로드한다** (ADR-0034). 스키마 변경은 ADR 필수.
5. **자원 등급을 잊지 않는다** (ADR-0034). ADR(판단·근거·실측) > 스펙 > 코드 > 구현에 묶인 테스트. **코드를 살리려고 스펙을 비트는 것이 이 프로젝트에서 가장 비싼 실수다.** 갈아엎을 때의 순서는 `실측/판단 → ADR → 스펙(+schema/) → 코드 → 계약 테스트`이고 역방향으로 가지 않는다.

## 스펙 목록

| 파일 | 내용 |
|---|---|
| `specs/00-overview.md` | 목표, 산출물 정의, 성공 기준 |
| `specs/01-script-template.md` | 대본 설계 규칙 (script.md 포맷, 서사 원칙, 결과 제약, 문체) |
| `specs/02-beat-schema.md` | 씬 계약 — 각 필드가 왜 그런가 |
| `specs/03-visual-rules.md` | 시각 연출 — 무엇을 어떤 기준으로 고르는가 |
| `specs/04-audio-rules.md` | TTS/SFX/BGM 룰 |
| `specs/05-pipeline.md` | 파이프라인 단계 정의 + 단계 간 I/O 계약 + 단계 독립 6원칙 |
| `specs/06-topic-research.md` | 소재 시드(기사), 소스 지위, 후행 팩트체크 (ADR-0049) |
| `specs/07-judgment-autonomy.md` | 판정 블록(`script.md` 맨 위 주석, ADR-0094) + human.json의 `video_line` — 나머지는 보류 (ADR-0049) |
| **`specs/schema/`** | **기계가 읽는 계약 11개.** 목록·소비자의 정본은 `specs/schema/README.md`다. 자주 여는 것: `vocab.json`(닫힌 어휘·스타일·영상 라인), `scene.schema.json`(씬 계약), `script-rules.json`(분량 엔벨로프), `speech-rules.json`(발화형 — ADR-0063), `beat-defaults.json`(연출 기본값) |

## 기술 스택 (확정분)

- Python 3.11+, FFmpeg (조립/자막 번인)
- LLM 단계: **구독 헤드리스 2종** (ADR-0008 → ADR-0097). **대본을 쓰는 `[1] draft`·`[2l] localize`는 codex(ChatGPT 플랜)**, 팩트체크·씬 분할·프롬프트 생성·검수는 Claude Code다. `--engine {claude,codex}`가 단계 기본값을 이긴다. 두 구독 다 한계비용 0이고 **한도 풀이 다르다** — `.env`에 `OPENAI_API_KEY`를 두지 않는다 (두면 codex가 종량제로 샌다)
- TTS: ElevenLabs 본인 목소리 클로닝 (ADR-0004)
- 시각: **씬당 영상 클립 1개.** **어댑터는 사람이 판정 게이트(`judgment/human.json`
  `video_line`)에서 고른 영상 라인이 정한다** (ADR-0059) — 로컬 GPU(ComfyUI + MiniMax H3,
  변동비 0, **초기 기본**) / 유료 API(Gemini Omni Flash, 인포 소재, 전환 조건 충족 뒤) /
  **`art`(일반 씬은 MJ 그림, `info` 씬은 H3 텍스트→영상 — ADR-0070·0071·0075)**. 라인
  목록·어댑터 표·라인별 값은 `vocab.json` `video_line`. 무대는 `studio`/`location` 둘이고
  **룩은 라인이 지되 엔진마다 말이 다르다** (전역 `style.base_style`은 세미 스타일라이즈드
  3D, `art`는 자기 `mj_style`·`ttv_style` — 앵커 기준 회화체, ADR-0075)
- **프레임을 입력으로 받는지는 라인이 아니라 씬이 정한다** (`style_in_frames`,
  ADR-0070·0071 → ADR-0075가 씬 단위로 좁혔다). `art`의 **일반 씬**만 `[6] frames`가 MJ로
  CLEAN 한 장을 그려 `[7]`이 그것을 first로 받고, 그 씬의 영상 프롬프트에는 STYLE 절이
  없다(스타일은 프레임이 진다). **`info` 씬은 프레임 없이 H3 텍스트→영상으로 가고 전체
  골격 + STYLE 절을 받는다.** ~~NB2가 계측 표시를 정지 이미지에 얹는 경로~~는 **폐기됐다**
  (ADR-0075 — 지표는 좋았지만 사람 판독에서 졌다). MJ는 **Pro 정액 활성**이다
- **`info` 씬의 정보는 표시가 아니라 구도가 진다** (ADR-0075). `vocab.json` `info_device`가
  그 구도 장치의 어휘이고(절단면 색분리·그림 속 지도·축척 인물·셀 수 있는 반복 등),
  빨간 계측 표시는 **그 위의 주석**이다. 근거는 앵커 14장 실측 — 빨간 지시선이 0장인데도
  전부가 무언가를 설명한다
- **화면 텍스트는 영어(ASCII)만** — 숫자·SI 단위·짧은 영어 라벨을 `info` 씬의 빨간 계측
  표시로 그린다. **영상 모델이 그리고 `[7]`의 씬당 비전 검수가 본다** — 못 그리면 RED 절을
  뺀 변종으로 강등하고 숫자는 내레이션·자막이 받친다. **끝 프레임 OCR 게이트는 꺼져 있다**
  (ADR-0068 — 잡음이 진짜 라벨보다 신뢰도가 높다). 한국어·일본어는 자막 번인으로만 화면에
  나간다 (ADR-0002 유지). 변동비는 라인의 것 — 로컬 0, 유료 API 토픽당 ≈$8(쇼츠 1편당
  ≈$2.7), `art`는 MJ 정액 + 로컬 GPU(`info` 씬 수에 비례)
- **토픽 하나 = 쇼츠 최대 3편** (ko·ja·en — **어느 언어로 만들지는 사람이 판정 블록에서 고른다**,
  ADR-0094). 대본은 한국어 정본 → 팩트체크 → `[2l]` 줄 1:1 번안. **클립은 기본 풀 하나 +
  언어별 겹풀 절반이다** (ADR-0095 — ja는 홀수 씬, en은 짝수 씬을 새로 산다. 같은 화면은
  두 번째 채널부터 죽는다는 실측이 근거다). TTS·자막·조립은 언어별이다. 목소리는 언어별 `.env` id
- 대본 생산: **기사 시드 → 통짜 대본(script.md) → 후행 팩트체크 → 번안, 토픽당 LLM 세션 3회** (ADR-0049·0056).
  영상 프롬프트는 `[5]` 세션 1회가 대본·씬 계약·팩트체크에서 원카랩식 SUBJECT 단락(영어, 설명의
  무대)을 쓰고 코드가 어휘 골격에 얹는다 (ADR-0060). 화면 라벨의 숫자는 그 줄이 말하는 숫자다.
  씬 분할·연출은 TTS 실측 뒤 `[3s]`. 검증은 기계 엔벨로프 검사 + 사람 판독 — LLM 채점 게이트 없음,
  재생성 루프 없음 (ADR-0044 원칙 유지). 2부 실행은 `[3] tts`가 첫 단계

## 작업 규칙

- 커밋 전 관련 스펙 파일 경로를 커밋 메시지 본문에 명시한다 (예: `spec: specs/03-visual-rules.md`).
- 새 파이프라인 단계 추가 시: 스펙(05) 수정 → ADR 작성 → 구현 순서. **새 단계는 기존 단계를 수정하지 않는다** (단계 독립 6원칙, specs/05).
- **어휘·enum·분량 값을 코드에 손으로 옮겨 적지 않는다.** `specs/schema/`에서 로드한다 (ADR-0034). `python tools/spec_audit.py`가 손 복사와 문서 간 불일치를 잡는다.
- 테스트: 각 단계는 픽스처 JSON 입력 → 스키마 검증 통과가 최소 기준. **계약 테스트는 살리고 구현에 묶인 테스트는 코드와 함께 버린다** (ADR-0034 §4).
- 한국어 텍스트가 최종 화면에 노출되는 경로(자막, 대형 숫자 텍스트)는 반드시 후처리 합성 경로를 탄다. 이미지 생성 모델에 정확한 한국어 렌더링을 기대하지 않는다 (ADR-0002).
- **콘솔 로그는 `runs/logs/`에 쓴다** — 배치·분리 프로세스·서버(ComfyUI 등)의 표준 출력이다.
  `runs/` 바로 아래에 흩뿌리면 run 디렉터리와 섞인다. 단계별 LLM 세션 로그는 코드가
  `runs/{run_id}/logs/`에 쓰므로 여기와 다른 자리다.
- 세션 1개 = 파이프라인 단계 1개 수준의 작업 1개.
- 세션 컨텍스트는 버릴 수 있게 유지한다. 남길 것은 `docs/worklog.md`에 쓴다 (훅이 갱신을 상기시킨다).
- **보고서·문서 산출물은 로컬 파일로만 만든다** (`reports/`, gitignore). **claude.ai 아티팩트로 게시하지 않는다** — 회사 계정이라 웹 클로드에 접근할 수 없어 링크를 열지 못한다.

## 레퍼런스 분석 결과 (요약)

원본 채널 3편 실측 기반. 상세 근거는 `docs/reference-analysis.md`.

- 길이·자수·발화 속도·줄 수의 **검증 기준은 `specs/schema/script-rules.json` 하나다.**
  줄당 속도·글자 수는 원본 3편 실측의 엔벨로프이고, 총량(초·글자·줄)은 사람 목표 60초 × 그
  속도의 유도값이다 (ADR-0057). 생 실측값 자체는 `docs/reference-analysis.md`에만 둔다. **여기에 값을 옮겨 적지 않는다** — 코드가 어느
  쪽이 계약인지 모르게 된다 (ADR-0034). `tools/spec_audit.py`가 잡는다
- 자막 1줄 ≈ 1씬 (ADR-0013)
- 하드컷보다 디졸브 위주. **전환 수는 계약이 정한다** — 씬 수 − 1이고 씬 수는 자막 줄 수라 분량 엔벨로프의 유도값이다. 세션이 고르는 것은 비율뿐이고, 관측 지표는 편당 하드컷 수다 (specs/03 「전환」). 레퍼런스의 "2.3~2.9초당 1회"는 장면 검출(scdet) 단위라 우리 씬 간 전환 수와 다른 값이다
- 카메라는 느린 줌/틸트/팬만 사용 (AI 영상 왜곡 방지)
- ~~시그니처 문구~~ — **폐기 (ADR-0050).** 그 문구들은 레퍼런스 채널의 브랜드다. 계약·프롬프트에서 주입을 끊었고, 우리 문구는 몇 편 만들며 자연 발생하면 사람이 정한다
