# worklog

세션을 마무리할 때 맨 위에 항목 1개를 추가한다. 역순 누적이다.
세션 시작 훅이 **맨 위 항목 1개만** 주입하므로, 다음 세션이 알아야 할 것만 그 항목에 담는다.

> ⚠ **`## `로 시작하는 줄은 항목 구분자다** (`.claude/hooks/session-start.ps1`이 그것으로
> 최신 항목을 자른다). **다른 용도로 `## `를 쓰지 말 것** — 쓰면 그게 "최신 항목"으로
> 주입되어 다음 세션이 인계를 못 받는다. 소제목은 `###`부터 쓴다.

**이 파일의 운영 규칙 (ADR-0034)**

worklog는 **인계장이지 아카이브가 아니다.** 판단과 근거는 ADR에, 지금 상태만 여기 둔다.

- **ADR에 있는 내용을 여기 다시 쓰지 않는다.** ADR 번호로 가리킨다 — 2026-08-13 (5) 항목이
  315줄까지 부푼 원인이 그 중복이었다. 훅이 맨 위 항목을 통째로 주입하므로 길이가 곧 비용이다
- 한 항목의 목표 분량은 **80줄 안팎**이다
- 항목이 **5개를 넘으면** 오래된 것을 `worklog-archive/`로 옮긴다.
  옮기기 전에 **그 항목에만 있는 판단이 없는지 보고, 있으면 ADR로 승격**한다
- 아카이브도 같은 리포에 남는다. `grep`으로 찾을 수 있으므로 유실이 아니다

| 파일 | 범위 |
|---|---|
| `docs/worklog.md` | 최근 5개 |
| `docs/worklog-archive/2026-08.md` | 그 이전 (월별) |

## 2026-08-25 (46) — **`art` 라인이 실측으로 섰다 (ADR-0069·0070).** MJ 그림체 복원 + first/last 전 구간 검증

사람 지시로 하루 종일 `art` 라인 프로브를 돌렸다. 판단·근거는 ADR-0069(프롬프트 예산)·
ADR-0070(first/last 구조)에 있다. **여기는 상태만 적는다.**

### 먼저 한 것 — (45)가 시킨 검증

**ADR-0067(고쳐쓰기)은 듣는다.** `us-penny-halt`가 사다리로 돈 첫 편이고 **고쳐쓰기 8회 중
6회가 다음 시도에서 통과**했다. `demoted_video` **2/18**(info 9/18)로, 사다리 없이 돈
korean-id 9/16(info 6/16)·edo 9/17(info 7/17)과 갈린다. **info 비율이 더 높은데 강등이 1/4**이라
(45)가 세운 "info 비율 = 재사용률" 상관은 깨진다. 원인은 info가 아니라 *같은 프롬프트 재시도*였다.
→ **22씬 재생성의 조건은 충족됐다. 아직 안 샀다.**

지난 세션 미커밋분(ADR-0066·0067·0068 포함)을 `403944d`로 커밋했다.

### 오늘 실측 (전부 ADR-0069·0070에 근거로 들어갔다)

- **프로브 1·2·3·4 전부 통과.** 영상 모델이 `last_frame`을 재현하고(31.4 dB), MJ 그림도
  움직이고(22.6), MJ가 계측 기하를 정확히 그리고, **MJ `endImage`가 우리 INFO 이미지를 받는다**
  (54초, 끝 프레임에 `River 340 m`·`1.1 km`가 글자까지)
- **Cloudflare R2를 세워 전달 문제를 닫았다.** 사람이 계정·버킷(`shorts-factory`)·토큰을 만들고
  `.env`에 `R2_*` 다섯 값을 넣었다. 프록시 `imageStorageType = LOCAL → R2`. 업로드가
  `https://pub-….r2.dev/...`(HTTP 200 · `image/png`)를 돌려주고 MJ가 받는다. **아웃바운드
  전용이라 이 PC가 안 열린다** — 중간에 쓴 임시 터널은 관리 API까지 열려 폐기했다
- **영상 프롬프트에 STYLE 절을 넣으면 중간 프레임이 무너진다** (9.5 dB → 빼면 13.6). ADR-0070 규칙 1
- **MJ 모드 실측**: fast 60초/GPU 1.8분 · relax 208초/GPU 0(동시 3) · **turbo는 영상 거절**.
  Pro 잔량 27.3h ≈ 900클립 ≈ 53편/월

### 프록시가 "죽은" 게 아니었다 — 원인 셋을 갈랐다

1. **프롬프트 길이** — 200단어면 MJ가 재작성하고 프록시가 텍스트로 못 묶어 3분 뒤 죽는다 (ADR-0069)
2. **`timeoutMinutes = 3`** — MJ 영상은 206초라 상한을 넘는다. **10으로 올렸다** (프록시 계정 설정,
   되돌리려면 같은 값 하나). 이걸 올린 뒤 영상이 바로 성공했다
3. 디스코드 모드·프록시 버전은 멀쩡했다 (`v11.10.4.0` 최신, 3일 전 같은 계정에서 성공 기록 있음)

**MJ 설정은 아무것도 안 없어졌다** — 어댑터(`imagegen/midjourney.py`)·ADR·`MJ_API_SECRET`·
샘플 25장 전부 있고, 삭제된 영상 어댑터도 `2f1b6bf^`에 온전하다. **CLAUDE.md의 "MJ 구독 해지"는
사실과 다르다** (Pro 활성) — (45)가 고치라고 남겼는데 아직 안 고쳤다.

### 구현했다 (ADR-0069·0070 승인 뒤)

사람이 승인해서 **계약 → 코드 → 계약 테스트** 순서로 넣었다 (CLAUDE.md 5).

- `vocab.json` — `meta.mj_dialect` 신설(어순·단어 예산·`::` 금지), `video_line.art`를 채웠다
  (`provider: mj-endimage` · `fallback_provider` · `style_in_frames` · **라인 고유 `base_style`**).
  **그림체는 사람이 P(셀셰이딩 2.5D)로 정했고** 그 문자열이 여기 들어갔다 — 전역 `style.base_style`
  (세미 스타일라이즈드 3D)은 그대로라 `local`·`api`는 안 바뀐다
- `specs/03` — 「스타일을 무엇이 지는가」·「MJ 방언」 두 절 신설. `specs/05` — 라인이 프레임을
  입력으로 받을 수 있다는 계약
- `schemas/vocab.py` — `mj_dialect()`·`line_style()`·`style_in_frames()`
- `schemas/visual_rules.py` — `build_mj_prompt()`·`check_mj_prompt()` 신설. **제출 전에 막는다**
- `videogen/midjourney.py` **부활** (ADR-0056이 지운 것) + `endImage`·`motion`·모드 선택.
  CLI에 `mj-endimage` 등록
- `tests/test_art_line.py` **15개 신설**, 전체 스위트 통과, `spec_audit` 0건

**남은 구현 (다음 세션)**: **CLEAN·INFO 두 장을 만드는 단계가 없다.** ADR-0070이 계약만
정했고 단계는 안 지었다 — 새 단계라 스펙 05 수정 + ADR이 먼저다 (단계 독립 6원칙).
특히 **계측 표시를 이미지에 자동으로 앉히는 방법이 미설계다** (오늘 프로브는 좌표를 손으로 박았다).
`comfy-h3-fl2v`(대체 엔진)도 미구현이다 — 템플릿에 `first_frame`/`last_frame` 배선이 필요하다.

### 사람이 고를 것 (오늘 답 안 난 것)

- **`staging: studio` 도해 충돌** — `base_style`의 "배경을 끝까지"가 스튜디오 공백과 싸워 도해가
  건축물이 된다. 별건 ADR
- **22씬 재생성** (korean-id 9 + edo 9 + rai 4) — 조건은 충족됐는데 `art` 라인이 확정되면
  라인 교체 대상이 될 수도 있어 보류 중

### 다음 세션이 알 것

- **ADR-0069·0070은 승인됐고 계약·코드가 들어갔다.** 0070의 엔진 기본값은 MJ `endImage`다
  (로컬 H3는 대체 엔진으로 유지)
- **프록시 설정이 두 자리 바뀌어 있다** (리포가 아니라 프록시 쪽): 계정 `timeoutMinutes` 3→10,
  전역 `imageStorageType` LOCAL→R2 + `cloudflareR2` 값. 되돌릴 때 볼 자리다
- ComfyUI가 한 번 죽었다. 재기동은 venv 파이썬 직접 실행이고 **공백 있는 경로를 인용해야 한다**
  (`--extra-model-paths-config "…\Comfy Desktop\…"`) — 안 하면 인자가 쪼개져 죽는다
- `zipper-late-adoption`이 `[5]`에서 여전히 막혀 있다 (`vocab.json` `_target_forbidden_words`의
  `fly`가 "trouser fly"를 오탐). 계약 값 문제라 사람 판단 대기
- 엔딩 크레딧 구분자 두부(`·` U+00B7), 위키미디어 429, `[10] mix` 미구현, (39) 석빙고 `[3]` 재실행
  — 전부 (45)에서 그대로 넘어왔다

## 2026-08-24 (45) — **첫 쇼츠 3개 국어 완성.** `[7]`이 처음 돌았고 **로컬 H3가 info 씬을 못 그린다**

사람 지시: "이전 세션에서 준비한 걸 계속 진행해서 유튜브 쇼츠를 끝까지". 중간에 영상 라인
선택을 물었고 사람 답은 **"일단 다 돌린다"**.

### 산출물

- **`korean-id-number` 완주** — `timeline.{ko,ja,en}.mp4` (60.9 / 74.6 / 61.1초, 싱크 오차 ≤5ms).
  **파이프라인 전체가 처음으로 끝까지 돌았다**
- `[3s]` 5편 전부 통과 (라벨-숫자 검사를 끈 결과 japan-5060hz가 풀렸고 나머지는 재시도로 됐다)
- `[4]`·`[5]` 5편 완료. **`[7]`은 나머지 5편이 아직 돌고 있다** (분리 프로세스, 아래)

### `[7]` 첫 실측 — ADR-0056 결정 6·ADR-0059의 관측값

`korean-id-number` 16씬에 **생성 38회**. 최종: 원본 5 · `demoted_info` 2 · **`demoted_video` 9**.
**절반 이상(9/16)이 인접 클립 재사용이다.** `japan-5060hz`는 4씬에서 3씬이 재사용이었다.

기각 사유가 연출 트집이 아니라 **모델 능력의 벽**이다:

- 50Hz·60Hz 사인파를 **같은 파장으로** 그린다 (촘촘함 차이가 그림의 목표인데)
- 지도에서 빨간 지시선을 **반대편에** 붙인다 (`Left bank`/`Right bank` 좌우 반전)
- 계약이 사인파를 요구했는데 각진 삼각 리본을 낸다

**즉 실사·풍경은 훌륭한데 `info`(계측 표시) 씬을 못 그린다.** info 비율이 곧 재사용률이다 —
korean-id 6/16 → japan 12/18 → zipper 14/17. 그래서 GPU 큐를 **info 적은 순서로 재정렬**했다.
소요는 씬당 15~20분(재생성 포함), 편당 5시간, 6편이면 **GPU 25~30시간**이다.
비전 검수 세션이 **구독 사용 한도에 17회** 걸렸다 — 다른 헤드리스 작업과 쿼터를 다툰다.

**ADR-0059 되돌릴 조건의 근거가 이걸로 섰다.** 판단은 사람 몫이다.

### `[8] ending`이 사진을 열 수 없었다 — 고쳤다

첫 실행에서 후보가 **전부** "파일이 존재하지 않아 확인할 수 없다"로 기각됐다. 원인은
`ending.py`가 `refs/3/01.jpg`(상대 경로)를 세션에 줬는데 **헤드리스 세션은 임시 디렉터리에서
돈다**(`claude_code.py`의 `cwd=workdir`). `Read`가 열 수 있는 경로가 아니었다.
`[7]`의 비전 검수는 처음부터 절대 경로 + `add_dirs`를 썼고 코드 주석은 "`[8]`과 같은
메커니즘"이라고 적혀 있었지만 `[8]`은 그렇지 않았다 — **`[8]`이 한 번도 안 돌아서 안 잡혔다.**

`prompt_ids()`·`relabel_verdicts()`를 넣어 프롬프트에는 절대 경로를 주고 기록은 상대 경로로
되돌린다. 계약 테스트 2개를 붙였다. 고친 뒤 세션이 실제로 그림을 보고 판정한다 —
korean-id는 그래도 4장 전부 기각이었다(증명사진 얼굴+13자리 노출 등, 정당한 기각).

### 그 밖

- **ADR-0066 작성** — (44)가 ADR 없이 바꾼 `total_seconds` `[50,80]`을 뒤늦게 기록했다.
  `spec_audit.py` 0건
- **빨간 테스트 3개 정리** — 라벨-숫자 검사 스위치를 계약에서 읽게 했다. 전체 스위트 통과
- `[7]`→`[8]`→`[9]`를 `--provider fake`로 **먼저 배관 검증**했다 (GPU 쓰기 전에). ADR-0065
  제목 훅·언어별 자막 폰트 전부 정상

### 재생성이 같은 프롬프트를 다시 던지고 있었다 — 고쳤다 (ADR-0067)

사람 지적: **"실패한 걸 똑같이 재시도하니깐 실패하는 거 같아. 재시도할 때 프롬프트를 수정해서."**
맞았다. `run_scene()`이 `variants`를 루프 **밖에서** 만들고 안쪽 재시도가 **같은 `prompt`
문자열**을 던졌다 — 바뀌는 것은 시드뿐이었고, 검수가 낸 `reasons`는 기록만 되고 아무 데도
돌아가지 않았다. `japan-5060hz` 씬 4가 "두 파형의 파장이 같다"로 **두 번 똑같이** 죽었다.

**고친 것** (ADR-0067):

- `[5]`가 세션 단락 3개(`subject_prompt`·`camera_target`·`red_prompt`)를 `prompts.json`에
  **싣는다.** 지금까지 조립본만 남아 `[7]`이 부분을 고칠 수 없었다 (`PROMPTS_SCENE_SCHEMA`에
  필드 추가, `subject_prompt`는 required)
- `[7]`이 기각 뒤 **고쳐쓰기 세션 1회**(`prompts/17-clipfix.md`)를 부른다 — 입력은 씬 계약 +
  단락 + **기각 사유**, 산출은 단락 3개. 코드가 `build_video_prompt`로 다시 얹는다
- **연출은 못 바꾼다** (ADR-0033 §3). 고친 단락은 `promptplan.cross_errors`로 다시 재고
  **어기면 되돌린다** — 사다리를 막지 않는다
- 계약 테스트 4개: 재시도 프롬프트가 실제로 달라지는가 / 계약 위반은 되돌아가는가 /
  옛 `prompts.json`(단락 없음)에서도 도는가 / 사다리에 고쳐쓰기가 낀 순서

**관측값(전)**: `demoted_video` korean-id 9/16 · rai-stones 4/18 · edo 9/17.

### ComfyUI가 죽어 두 토픽이 재시도를 태웠다

22:19에 ComfyUI 서버가 죽었고(프로세스 소멸), `[7]`이 `exit=17`(프로바이더 전체 거절)로
**몇 초 만에 재시도 3회를 다 태우고** us-penny-halt·japan-5060hz를 접었다. 클립은 남아 있다
(11개·3개). `gpu.sh`에 **`wait_comfy()`**를 넣어 17이면 재시도를 태우지 않고 최대 30분 기다린다.

**주의**: `ComfyUI.exe`(Desktop)를 직접 띄우면 **설치 마법사로 들어간다**(`Install state: not
installed`) — 서버가 안 뜬다. 서버만 필요하면 venv 파이썬을 직접 돌린다:
`…\Comfy-Desktop\ComfyUI-Installs\ComfyUI\ComfyUI\.venv\Scripts\python.exe -s ComfyUI\main.py --port 8188`
(작업 폴더는 `…\ComfyUI-Installs\ComfyUI`). 기존 설치는 멀쩡했다.

### 엔딩 크레딧 구분자가 두부로 나온다 (미수정 — 사람이 고를 것)

`rai-stones`가 **엔딩 3컷을 처음으로 만들었다**(사진 3장·크레딧 번인 3). 그런데 화면 크레딧이
`Eric Guinther, 2002, 야프 가지파르 마을 □ CC BY-SA`로 나온다.

원인: `video/ending.py`의 `credit_line()`이 구분자로 **`·`(U+00B7)**를 쓰는데,
`find_font()`가 고르는 `sorted()[0]` = **DoHyeon-Regular.ttf의 cmap이 U+00B7을 `summation`
글리프에 잘못 매핑**해 뒀다. 폰트에 글자가 없는 게 아니라 **매핑이 틀린 것**이다.

**표시 의무는 지켜지고 있다** — `ending/credits.txt`가 정본이고 거기는 멀쩡하다 (ADR-0055가
번인 강등과 파일을 갈라 둔 이유가 이거다). 그래서 실패가 아니라 미관 결함이다.
고르는 자리가 둘이라 사람 몫으로 남긴다: **구분자를 바꾸든**(`,`·`-`) **폰트 선택 규칙을
바꾸든**(`sorted()[0]`은 임의다). 지금 상태로 엔딩이 붙는 편은 전부 이 글자가 나온다.

### 다음 세션이 **먼저** 할 일 — 완성 3편의 재사용 씬을 다시 만든다 (사람 지시)

사람이 화면 반복을 알아봤다: **"1번 씬이 2번 반복되는 그런 현상"** — 인접 클립 재사용
(`demoted_from: video`)이 그대로 보인다. 지시: **"2번을 해야 하는데 그건 worklog에 남겨,
지금 하던 거 끝내고 세션을 다시 시작하면서."**

완성 3편의 재사용 실태 (고쳐쓰기 **전**에 만들어진 것들이다):

| 토픽 | 씬 | 재사용 | 눈에 띄는 구간 |
|---|---|---|---|
| korean-id-number | 16 | **9** | 씬 3 화면이 1·2·3·4에 연속 (도입 4씬 ≈ 15초가 한 화면) |
| edo-night-soil | 17 | **9** | 씬 11 화면이 11·12·13·14에 연속 (4번) |
| rai-stones | 18 | 4 | 2곳씩 2번 반복 4군데 |

**하는 법** — 살아 있는 클립은 건드리지 않는다. `clips.json`·`clip_review.json`에서
`demoted_from == "video"`인 씬 기록만 빼고 `clips/{id}.mp4`를 지운 뒤
`python run.py videogen --slug X` (**`--force` 금지** — force는 done 씬을 전부 버린다).
그다음 `[9] assemble --force`. 이 세션이 us-penny-halt·japan-5060hz에 같은 절차를 썼고
`_existing_done()`이 의도대로 남은 씬만 다시 산다. 총 22씬 ≈ 2~3시간.

**단, 먼저 볼 것**: us-penny-halt·japan-5060hz가 **고쳐쓰기 사다리로 처음 나온 편**이다.
그 둘의 `demoted_video` 비율을 위 표와 비교해서 ADR-0067이 실제로 듣는지 확인한 뒤에
22씬을 다시 사라 — 안 들으면 같은 결과를 두 번 사는 것이고, 그때 움직일 곳은 영상 라인이다.

### 비용은 돈이 아니라 **구독 쿼터**다 (사람 질문)

오늘 실측: **비전 검수 세션 120회**(씬 65개 — 시도마다 1회), 단계 세션 토픽당 7회(×6 = 42회),
고쳐쓰기 3회. **구독 한도에 걸려 대기한 것이 18회.** GPU 렌더는 로컬이라 변동비 0이다.
압도적 비중이 `[7]`의 씬당 비전 검수이고, **ADR-0067의 고쳐쓰기가 기각 1회마다 세션을 1회
더 얹는다** — 재사용을 줄이는 대가다. 남은 28씬 + 재생성 22씬이면 검수·고쳐쓰기가 100회
단위로 더 붙는다. 쿼터가 빡빡하면 줄일 곳은 `--review ocr`이 아니라(그건 게이트를 없앤다)
**한 번에 도는 토픽 수**다.

### `art` 라인(MJ)의 길이 열렸다 — **H3가 원래 first-last 모델이다** (사람 방향 결정)

사람 질문에서 시작했다: "H3가 시작 프레임과 끝 프레임 넣어서 영상 만드는 게 있는지 확인해
볼래? 그거 있으면 NB로 인포그래픽 만들어서 끝 프레임으로 쓰는 거, 예전에 기각한 방식으로
가능할 거 같은데."

**확인했다 — 있다. 그리고 그게 이 모델의 본업이다.**

- 모델 파일이 `minimax_h3_**fl2v**a_pruned_int8_convrot.safetensors` — **f**irst-**l**ast **2** **v**ideo
- 노드 `MiniMaxH3ImageToVideo`의 입력에 `first_frame: IMAGE`·`last_frame: IMAGE`가 **둘 다
  optional로 열려 있다** (`/object_info`로 확인)
- **우리 템플릿(`config/comfy/h3-t2v.api.json`)은 둘 다 안 물린 채 t2v로만 쓰고 있다** —
  모델이 할 수 있는 것의 절반만 쓰는 중이다

**그리고 그 설계는 이미 ADR-0043**(「인포씬 경로 — MJ CLEAN + NB2 인포 편집 + Veo first/last」)
**에 문서화돼 있다.** ADR-0056이 그것을 폐기했지만 **품질이 아니라 돈 때문이었다**:
Veo 인포씬 편당 $7~10 · MJ Pro 정액 $60/월, 그리고 MJ 이미지 단계를 지우니 NB2의 소비자가
사라졌다. **그 두 이유가 지금은 안 선다** — first/last를 **로컬 H3가 변동비 0으로** 한다.

### 사람 방향 결정 — `art` 라인은 **정지가 아니라 영상**이고, 엔진은 H3다

> "방향은 MJ로 **영상** 만드는 방향이야. 루트가 3루트가 있어 — H3 로컬생성, 유료 생성,
> MJ는 NB2로 밑그림 후 유료였는데 금액 때문에 다른 방법을 찾는 중이었는데 지금 H3로
> 가능하다니 그걸 루트로 하자. **단 나중에 H3를 유료로 바꿀 수 있다는 것만 남겨 줘.**"

| 라인 (`vocab.json` `meta.video_line`) | 어댑터 | 지금 |
|---|---|---|
| `local` | `comfy-h3` | 쓰는 중 (t2v). 변동비 0 |
| `api` | `omni` | 인포 소재용, 전환 조건 대기 |
| **`art`** | **`null` (미구현)** | **MJ 밑그림 → NB2 인포 편집 → H3 fl2v.** 이 방향으로 간다 |

**`art`는 MJ 정지 이미지 쇼츠가 아니다** — 켄번스 정지로 되돌아가는 것이 아니라 **MJ가 그린
밑그림을 H3가 움직이는 영상 라인**이다. 구조:

    MJ CLEAN 이미지(글자·그래픽 없음)  →  first_frame
            ↓ NB2 편집 (지시선·라벨·수치를 얹는다)
    INFO 이미지                        →  last_frame
            ↓ H3 fl2v (로컬, 변동비 0)
    지시선이 조립되며 완성되는 클립

**왜 이 구조가 오늘의 실패를 피하는가**: `[7]` 첫 실측에서 H3는 "정확한 계측 기하를 상상해서
그리는 일"에 실패했다(파장이 같은 사인파, 좌우 반대 지시선 — 항목 위쪽 참조). 이 구조는
H3에게 그것을 안 시킨다. **숫자·라벨의 정확성은 NB2가 정지 이미지 한 장에서 지고**, H3는
"이 그림에서 저 그림으로 가라"만 한다. 정지 이미지는 검수·재생성이 훨씬 싸고 확실하며,
끝 프레임이 고정되므로 **`[7]`의 끝 프레임 OCR 게이트가 비로소 제 역할을 한다**.

**남겨 두는 것 (사람 지시)**: **H3 자리는 나중에 유료로 바뀔 수 있다.** 이 결정은 "first/last
구조로 간다"이지 "H3에 묶인다"가 아니다 — `art` 라인의 `provider`는 다른 fl2v 어댑터로
갈아 끼울 수 있어야 하고, ADR을 쓸 때 그 여지를 계약에 남긴다.

**아직 모르는 것 — ADR 전에 프로브로 답할 것 (2회면 된다)**

1. **H3가 `last_frame`을 얼마나 충실히 지키는가.** 끝 프레임을 재현하는가, 참고만 하고
   흘리는가. 이게 아니면 구조 전체가 안 선다 — **먼저 잰다**
2. **NB2가 CLEAN 위에 계측 표시를 정확히 얹는가.** ADR-0043 시절 검증 여부 확인 필요.
   `GEMINI_API_KEY`는 살아 있다

**손대지 않았다** — 되돌리기 비싼 결정이라 ADR이 먼저다 (CLAUDE.md 2). 템플릿·어휘·어댑터
어느 것도 안 고쳤다.

### MJ는 **구독이 살아 있다** — CLAUDE.md가 틀렸다 (사람 정정)

> "MJ 구독이 해지 안 됐어. **도커를 기동시켜야 되는 거야.** 첨부터 MJ를 사용해서 테스트하는
> 걸로 하자."

**`CLAUDE.md`의 「MJ 이미지 어댑터는 휴면 코드(호출자 없음, **구독 해지**)」가 사실과 다르다.**
호출자가 없는 것은 맞지만 구독은 살아 있고, 안 되는 이유는 **`mjopen` 프록시 도커가 안 떠
있어서**다. 다음 세션이 `CLAUDE.md`의 그 괄호를 고칠 것 — 이 세션은 사람이 worklog에만
남기라고 해서 고치지 않았다.

기동 조건 (`imagegen/midjourney.py`, ADR-0025):

- 프록시 `mjopen`을 **도커로 띄운다** → `MJ_PROXY_URL` (기본 `http://127.0.0.1:8086`)
- 인증 헤더 `mj-api-secret` ← `.env`의 `MJ_API_SECRET` (**있다**)
- 제출 `POST {base}/mj-fast/mj/submit/imagine` · 폴링 `GET {base}/mj/task/{id}/fetch`
- **이미지는 `/mj-fast/` 프리픽스다** (ADR-0039이 ADR-0025 §2를 뒤집었다). 프리픽스를 틀리면
  잘못된 프롬프트가 아니라 **다른 엔드포인트**가 된다
- 응답의 `imageUrls`는 `{url, thumbnail}` **객체 배열**이고 최상위 `imageUrl`은 2×2 그리드다.
  둘을 헷갈리면 씬마다 그리드가 들어간다 (실측 주의)

### `art` 루트 첫 테스트 — **japan-5060hz, 처음부터 MJ로** (사람 결정)

사람: "japan-5060hz 이건 MJ 테스트하기 딱 좋은 토픽 같은데" → **동의**. 그리고 밑그림은
NB2 대체 없이 **처음부터 MJ**로 간다 (사람 결정).

**왜 이 토픽인가**

- **최악 조건이다.** info 12/18로 두 번째로 높고, H3 t2v에서 4씬 중 3씬이 재사용으로 떨어졌다
- **실패 기록이 대조군으로 남아 있다.** 같은 씬을 다른 루트로 돌려 직접 비교할 수 있다:
  - 씬 4 — "두 파형의 파장이 사실상 동일" (2회 연속 같은 실패)
  - 씬 2 — `Left bank`/`Right bank` 좌우 반전
  - 씬 3 — 지도 색 경계를 엉뚱한 쪽에, 지시선이 빈 바다에
- **1부 입력이 전부 있다** — `scenes.json`·`prompts.json`(ADR-0067 단락 포함)·실측·나레이션·
  `factcheck.md`. 새로 만들 것이 없다
- `[7]`을 중단시켰으므로 `clips.json`에 씬 2 하나만 남아 있다 (사람 지시로 종료)

**범위는 18씬이 아니라 씬 하나로 시작한다.** 모르는 것이 둘인데 둘 다 1~2씬이면 답이 난다:

1. **H3가 `last_frame`을 얼마나 지키는가** — 이게 아니면 구조 전체가 안 선다. **제일 먼저**
2. **NB2가 CLEAN 위에 계측 표시를 정확히 얹는가**

**프로브 대상은 씬 4(사인파)를 권한다** — 판정이 객관적이다. "파란 파형 12마루 / 주황 10마루"가
끝 프레임에 그대로 있으면 성공, 아니면 실패. 애매할 여지가 없고 H3 t2v가 정확히 여기서
두 번 죽었다. 두 번째 프로브는 씬 2(좌우안 지시선)로 "좌우 관계"까지 본다.


### tesseract를 처음 설치했고, 재 보니 **OCR 게이트는 기각이다 (ADR-0068)**

사람이 관리자 권한으로 깔았다 (`winget install UB-Mannheim.TesseractOCR`, v5.4.0.20240606,
`C:\Program Files\Tesseract-OCR`). 석빙고 때부터 계속 건너뛰어지던 게이트를 **처음으로 잴 수
있었다.** 사람 지시: "잡음 문턱 튜닝까지 해줘. **OCR이 못 잡으면 OCR은 기각.**"

**실측 (표본 130장, 토큰 0 — 완성 편들의 시도별 끝 프레임. 시도 단위라 재사용에 안 오염된다)**

게이트를 켰다면 **130장 중 6장(4.6%)만 통과**한다. `info` 씬 46장 중 5장, 글자 없어야 하는
84장 중 1장. 나머지 124장이 헛되이 재생성 사다리를 탄다 — 아끼려던 토큰이 폭증한다.

**문턱으로 못 살린다. 잡음과 진짜가 역전돼 있다:**

| | 예시 | conf |
|---|---|---|
| 잡음 (글자 없는 화면) | `67665` `WE` `998` `5300` `9239` | **92~96** |
| 진짜 라벨 | `High` `Mid` `Low` `Capacity` | **0** |

- 신뢰도 문턱: 96으로 올리면 진짜가 죽고 0으로 내리면 잡음이 산다 — **분리하는 값이 없다**
- 길이 문턱: 잡음 1~15자(2자가 802개), 라벨 3~13자 — 완전히 포개진다
- **라벨 재현율 36%**(69개 중 25개)인데 게이트는 라벨이 **전부** 읽혀야 통과다
- 잡음의 여러 자리 숫자가 `info` 씬의 "계약에 없는 숫자" 조항을 매번 때린다

**한 것** — ADR-0068 작성 → `script-rules.json` `checks.end_frame_ocr: false` →
`videogen.py`가 계약에서 로드(`END_FRAME_OCR`). **코드는 안 지웠다**: 게이트 동작을 검증하는
테스트 6개는 `skipif(not END_FRAME_OCR)`로 **휴면**시켰다 — `art` 라인에서 되살릴 때 그대로 쓴다.
전체 스위트 통과(6 skip), `spec_audit` 0건.

**남는 위험**: 화면 글자를 기계가 대조하는 수단이 **0개**다. 라벨 철자가 틀려도 비전 세션이
놓치면 나간다. 비전은 좌우 반전·파장 오류 같은 **의미** 오류는 잘 잡았지만 철자를 재는 축이 아니다.

**되살릴 조건**은 ADR-0068에 적었다 — 핵심은 `art` 라인이다. `last_frame`이 **우리가 만든 인포
이미지**가 되면 OCR이 보는 대상이 "H3가 상상해 그린 질감 화면"에서 "글자를 의도적으로 얹은
깨끗한 이미지"로 바뀌므로 위 실측이 그대로 적용되지 않는다. 그때 스위치를 켜고 다시 잰다.

**MJ 루트의 토큰 절감은 여기서 나온다** (사람 질문 "MJ로 하면 토큰 낭비가 줄어들까?"에 대한 답):
MJ 자체로는 안 준다 — 검수 대상이 CLEAN·INFO·영상 셋으로 **늘어난다**. 진짜 절감은 ①재시도
감소(오늘 재시도 60회의 다수가 인포 실패였고 그 실패가 사라진다) ②정지 이미지는 프레임 1장
(지금은 시도마다 3장, 오늘 390장 올라갔다) ③**인포 검증을 유료 세션에서 무료 기계 검사로 이관**.
③이 제일 큰데 지금은 위 실측 때문에 막혀 있고, `art` 라인이 그것을 푼다.

### 다음 세션이 알 것

- **`zipper-late-adoption`이 `[5]`에서 막혀 있다 (5회 전부 동일 실패).**
  `vocab.json` `meta.camera._target_forbidden_words`의 **`fly`**가 `camera_target`의
  **"trouser fly"(바지 앞섶)**를 오탐한다. 소재가 지퍼라 세션은 계속 그 단어를 쓴다.
  **계약 값 문제라 사람 판단이 필요하다** — 스펙 수정 제안이지 코드 회피가 아니다 (CLAUDE.md 1)
- **GPU 라인이 분리 프로세스로 돌고 있다.** 순서는 korean-id(완료) → rai-stones →
  edo-night-soil → us-penny-halt → japan-5060hz → zipper(마커 없어 건너뛴다).
  로그 `runs/gpu-batch.log`, 스크립트는 잡 tmp에 있다.
  **Bash 백그라운드 잡은 10분에 죽고 "killed"가 자식을 안 죽인다** — 재실행 전에
  `Get-CimInstance Win32_Process`로 확인하고 죽인다 (한 번 이중 실행이 났다)
- `japan-5060hz`는 4씬까지 갔다가 재정렬로 중단됐다. 재개하면 그 씬들을 다시 산다
  (`needs_reuse`는 done이 아니다)
- **tesseract가 여전히 없다** — `[7]` OCR 게이트가 통째로 건너뛰어진다. winget user-scope
  설치가 안 된다(머신 스코프=관리자). 비전 검수만으로 돌고 있다
- **위키미디어가 HTTP 429를 낸다** — `[4]`가 사진을 절반쯤 못 받고 서술만 쓴다.
  그만큼 `[8]` 재고가 얇다
- `[10] mix`는 여전히 미구현이라 끝은 `timeline.{lang}.mp4`다
- (39)의 석빙고 `[3]` 재실행은 **여전히 안 했다**

## 2026-08-24 (44) — **6편 판정 게이트 열고 `[3]` 완주.** `[3s]`의 라벨-숫자 검사를 껐다

사람 지시: "새 토픽 6개를 2부 끝까지". 2부 진입까지 갔고 `[7]`은 아직 한 번도 안 돌았다.

### 한 것

- **판정 게이트 6개 개방.** `topics/*/judgment/human.json`(judge=human · decision=go ·
  `video_line: local`) + `STATUS.md` `# STATUS: go`. 사람 일괄 지시가 근거라 scores·confidence는
  비웠다. 사람이 정한 둘도 human.json `notes`에 적었다 — edo는 표현 수위 "그대로",
  korean-id-number는 "완전한 13자리를 화면에 세우지 않는다"
- **`[3] tts` 6편 done** (ko·ja·en 18개 파일)
- **`[3s] scenetable`은 korean-id-number 1편만 done.** 나머지 5편 미실행

### 분량 계약을 넓혔다 — `total_seconds` `[54,66]` → `[50,80]` (ADR 미작성)

`[3]`이 ja에서 줄줄이 막혔다. 사람 판단: **"60초 내외라고 했지 50~80초 다 상관없으니깐
대본 축약하지 말고 그냥가."**

- `specs/schema/script-rules.json` `limits.total_seconds` `[50, 80]`
- `locales.ja` 임시 블록(`[54,75]`, ADR-0057 결정 2) **삭제** — 새 상한이 덮는다
- `total_chars`·`line_count`는 **안 건드렸다.** 목표 60초는 그 값들이 진다. `total_seconds`는
  이제 유도값이 아니라 `[3]` 실측 허용 폭이다
- **ADR-0066을 아직 안 썼다.** 계약이 먼저 바뀐 상태다 — 다음 세션이 쓴다

**실측 6편 ja**: 81.5 / 78.5 / 82.1 / 75.9 / 74.7 / 77.5. 넘친 두 편(edo·korean-id)은
**대본을 안 고치고 `--tempo 1.18`로** 돌렸다 (specs/04의 1.1~1.2 안). 결과 ja 69.7 / 74.6,
ko는 61.6 / 60.9로 오히려 목표 60초에 붙었다. **같은 대본도 실행마다 원속이 ±3% 흔들린다** —
korean-id ja가 79.4 → 82.1이었다. 상한 언저리는 재실행으로 못 넘긴다

### `[3s]`의 라벨-숫자 검사를 껐다 (`checks.label_numbers_from_line: false`)

ADR-0060 결정 4의 검사가 **한국어 수사를 못 읽어** 6편 중 2편을 통째로 막았다.

- korean-id-number: "**열두** 자리" 줄에서 라벨 `12 digits` 반려 (사전에 `열`=10만 있어 12가 안 잡힌다)
- japan-5060hz: "변환소가 전국에 **네 곳**뿐" 줄에서 라벨 `4 sites` 반려
  (한 글자 수사 한·두·세·네는 조사·어미 오탐 때문에 일부러 빠져 있다)

**사람 결론: 형태소 검사를 하지 마라.** 수사를 숫자로 읽으려면 형태소 분석이 필요하고 그 길로
가지 않는다. 스위치를 끄는 것으로 끝냈다 — 듣는 숫자와 보는 숫자를 맞추는 일은 **사람 판독**이 진다.
`schemas/scenes.py`는 손대지 않았다(되돌렸다).

### 다음 세션이 알 것

- **`[7]`은 아직 0회다.** 남은 일: 5편 `[3s]` → 6편 `[4] [5] [7] [8] [9]`. `[10] mix`는 미구현이라
  끝은 `timeline.{lang}.mp4`다
- **`test_scene_with_unspoken_label_number_is_rejected`가 빨간불일 것이다** — 스위치를 끈 결과다.
  전체 스위트를 다시 안 돌렸다
- **korean-id-number의 `scenes.json` 씬 4를 손으로 고쳤다** — 라벨 `110101-100001` →
  `First number issued`, visual_goal도 번호 생김새를 안 그리게. 사람 제약 반영이고 검증 0건이다
- **`[4] refpack`·`[8] ending`은 여전히 0회다.** 사람이 "6편 다 넣는다"로 정했다
- `[3s]`가 6편 다 "transition이 빈 씬 1개" 경고를 낸다
- tesseract가 PATH에 없어 `[7]` 끝 프레임 OCR 게이트가 건너뛰어진다 (석빙고 때부터)
- 배치 스크립트는 `scratchpad/run-part2.sh`에 있다. **한 단계 실패로 토픽을 접는 정책은 틀렸다** —
  `[3s]`는 확률적이라 재시도가 맞고, GPU가 노는 것도 그래서였다
- (39)의 석빙고 `[3]` 재실행은 **여전히 안 했다**

## 2026-08-24 (43) — **소재 18편 발굴 + 6편 1부 완주.** 판정 게이트 대기

병행 세션의 (40)~(42)와 같은 날의 다른 줄기다. 이 항목의 앞부분은 **(39)**(ADR-0063 발화형)이고,
그 항목의 "`[3]` 재실행 대기"는 **아직 그대로다**.

### 1. 소재 18편이 백로그에 섰다 (8차·9차 발굴)

사람 주문: "사람들이 몰라도 되는데 흥미로운 것". 8차는 도메인별(경제·종교·일상 각 2),
9차는 한국·일본 타겟(각 도메인 2씩 12편).

**시드 두께를 감으로 잡지 않았다.** 채택된 시드 14개를 먼저 실측해 기준선을 만들었다 —
위키텍스트 바이트로 CJK 12,638~116,303B, en 166,688B, 최저 예외가 갑인자 4,969B다.
그 위에서 후보 41개를 재고 상위만 본문을 받아 **훅이 실제로 문서 안에 있는지** 확인했다.

- 1차 선별: `action=query&prop=revisions&rvprop=size` — 한 요청에 50개까지 묶인다.
  `prop=extracts`는 전문 추출이면 exlimit이 1로 강등돼 못 묶는다 (429도 잘 난다)
- 나무위키: `stages/seedfetch.render_dom` + `extract_text`를 그대로 재사용했다 (새 도구 없음)
- **스펙 06의 "나무위키 먼저"가 실측으로 증명됐다** — 연은분리법은 한국어 위키백과 1,765B인데
  나무위키 16,301자다
- 탈락 사유가 기록된 것들: `220V 승압`(나무위키 `전압`이 세계 전압사 중심이라 한국 대목이
  지나가는 언급뿐 — 석빙고 패턴), `제사`(홍동백서 통념을 문서가 오히려 반박해 confidence 흔들림),
  `손 없는 날`·`삼성각`·`불복장`(얇다)

**백로그에 아직 후보 12편이 남아 있다** — 에루브·키블라·QWERTY·당백전·연은분리법·석굴암·
단청·에스컬레이터·도지마 쌀 시장·식년천궁·폐불훼석·다다미.

### 2. 여섯 편이 1부를 마쳤다

`[0]`→`[0f]`→`part1`(18세션). 전부 엔벨로프 안이고 ja/en 줄 1:1 정렬도 맞다.

| slug | 줄 | 자 | 최장 |
|---|---|---|---|
| `edo-night-soil` | 17 | 376 | 26 |
| `japan-5060hz` | 18 | 378 | 23 |
| `korean-id-number` | 16 | 363 | 24 |
| `rai-stones` | 18 | — | — |
| `us-penny-halt` | 18 | 384 | 23 |
| `zipper-late-adoption` | 17 | 383 | 29 |

### 3. 수미상관 검사기의 오탐을 고쳤다 (ADR 없음 — 버그 수정)

`rai-stones`가 `[1]`·`[2]`에서 연속으로 "엔딩이 훅의 명사를 하나도 재사용하지 않는다"로
막혔는데 **대본은 멀쩡했다.** 훅 "돈을 **바다에** 빠뜨렸는데" / 엔딩 "**바다** 밑 그 돌은 …
주소만 **바다**였죠"인데 교집합이 0이었다 — `noun_stems`가 조사 없이 다/아/어로 끝나는 명사를
용언으로 보고 버리기 때문이다(그 한계는 함수 독스트링에 '바다'를 예로 들어 이미 적혀 있었다).

`stages/scriptmd.py`에서 **엔딩 쪽 형태소 추정을 뺐다** — 훅에서 뽑은 명사가 엔딩 텍스트에
글자로 있는지만 본다. 판정이 느슨해지기만 하므로 전에 통과하던 대본은 그대로 통과한다.

**테스트는 없다 (사람 판단).** 처음에 붙였다가 "대본 문장을 그대로 박은 오버피팅"이라
지적받아 지웠고, 사람 결론은 **"형태소 분석 없이는 이 성질을 제대로 못 재니 테스트로는 과하다"**
였다. 기존 실패-경로 테스트(`test_ring_composition_failure_is_an_error`)는 훅의 명사가 글자로
아예 없는 경우라 형태소 판단이 필요 없어 남겼다.

### 4. 다음 세션이 알 것

- **여섯 편 다 `judgment/human.json` 대기다.** 사람이 go/no-go + `video_line`을 적어야 2부에 든다
- **`edo-night-soil`은 표현 수위를 사람이 정해야 한다.** 프레이밍 지침을 안 넣었는데도 `[1]`이
  시장 축(등급→수거권→투기→공급 과잉→붕괴)을 잡았고 매체 적합성도 스스로 "적합"으로 판정했다.
  다만 제목이 "에도의 똥에는 등급이 있었다"이고 첫 줄도 "똥"으로 시작한다 — 순화 여부가 판단거리다
- **`korean-id-number`는 화면 번호 제약이 필요하다.** 나레이션 본문에는 실제 형식의 13자리가
  없지만(주장 목록에만 있다), "보이는 것"이 *13칸 분해와 검산 계산*이라 `[3s]`의 info 라벨이
  체크섬 맞는 번호를 세울 수 있다. 실존 번호와 겹칠 수 있으니 `[3s]` 전에 제약을 정한다
- **`rai-stones`의 `state.json`에 실패 이력이 남아 있다** — `1-draft`가 failed인 채로 `2`·`2l`이
  done이다. 검사기를 고치기 전의 기록이고 대본은 지금 errors 0 · warnings 0이다
- **(39)의 `[3]` 재실행은 아직 안 했다** — 석빙고 `python run.py tts seokbinggo --force` → `[9]`.
  발화형이 실제 오디오에 반영된 것을 아직 아무도 못 들었다
- 병행 세션이 `e360d83`("브랜치 미커밋분 정리")에서 이 세션의 미커밋분을 함께 커밋했다.
  **이 worklog 항목만 미커밋이다**
- 긴 세션은 `Start-Process`로 분리해 띄우고 로그를 폴링하는 방식이 이번에도 맞았다
  (`runs/part1-batch.log`). 편당 약 16~20분, 6편에 98분이다

## 2026-08-24 (42) — **제목 훅 신설 (ADR-0065).** 엔딩 실사가 없던 이유는 「단계를 안 돌렸다」

사람이 둘을 요청했다. **하나는 이미 있었고 하나는 새것이었다.**

### 엔딩 실사 — 이미 구현돼 있다. 안 돌렸을 뿐이다

사람이 "석빙고 마지막에 없던데? 조립할 때 빠진 거 아니면 사진을 못 구한 건가"라고 물었다.
**둘 다 아니다.**

- `state.json` 단계 키가 `0, 1, 2, 2l, 3, 3s, 5, 7, 9`다 — **`4-refpack`·`8-ending`이 없다.**
  실패 기록조차 없으니 시도 자체를 안 했다
- `runs/` 27개 전체에 `refs.json` 0개 · `ending.json` 0개
- `[9]`는 `ending.json`이 없으면 **조용히 엔딩 없이 조립한다** (D-3). 경고 없이 `ending_cuts=0`

`[4] refpack`(476줄) → `[8] ending`(586줄) → `[9]`의 배선은 ADR-0055부터 다 있고 테스트 59개가
돈다. CLI도 있다. **파이프라인 순서에서 통째로 건너뛴 것이다.**

> **다음 세션이 할 것**: `python run.py refpack --slug seokbinggo` →
> `python run.py ending --slug seokbinggo` → `[9]` 재실행. 구독 세션 2회(웹검색 1 + 비전 1),
> 변동비 0. `[4]`는 `scenes.json` 하나만 있으면 되고 석빙고엔 있다.
> **한 번도 안 돌린 단계라 실제 웹·ffmpeg 경로에서 깨질 수 있다** — 테스트는 그걸 못 본다

### 제목 훅 — ADR-0065 신설·구현 완료

`# 제목`은 `scriptmd.py:143`이 이미 파싱하는데 **소비자가 0개였다.** `[2l]`이 세 언어로 번안까지
해 두는데 화면에 안 나갔다. 판단·대안 비교는 ADR-0065에 있다. 여기엔 상태만.

- **실측이 설계를 정했다** — 정규화된 폰트(ADR-0064)로 기존 제목 31개를 재니 **전부 자막과 같은
  44px·2줄에 들어간다**(ko 최장 45자 1,239px / ja 28자 1,194 / en 70자 1,583, 안전폭 990×2).
  그래서 **제목 전용 폰트 크기도 1부의 제목 길이 상한도 안 만들었다.** 새 계약값은 `title.band` 하나
- 흐름: `[3] tts`가 `scenes.timed.{lang}.json`에 **선택 필드 `title`** → `[9]`가 ASS에 `Title`
  스타일 + 이벤트 1개. 구간은 `0` ~ 첫 씬의 `end`(사람이 고른 값 — 상수 없음), 자리는 상단 중앙
- **`parse_ass`가 `Default`만 되읽는다** — 제목이 큐 목록에 섞이면 씬 수가 하나 늘어 싱크
  검증(±200ms)이 통째로 깨진다. 이게 이번 구현에서 제일 조용한 함정이었다
- 강등 사다리 `굽기 → 없음` — 안 들어가면 제목만 빼고 경고. 자막은 같은 상황에서 실패지만
  제목은 마감이지 본편이 아니다

**검증**: 석빙고 실편 + 실제 제목으로 ASS 생성 → ffmpeg 실번인 3장. 세 언어 다 2줄,
제목 밴드 0.140~0.195 · 자막 밴드 0.765~0.820으로 안 겹친다. `Dialogue` 17줄인데 `parse_ass`는
16큐(= 씬 수). **1,092개 통과, `spec_audit` 0건.**

### 알아 둘 것

- **ja 제목의 줄바꿈이 불균형하다.** `石氷庫 —` / `電気のない朝鮮王朝が真夏に氷を食べた方法` —
  `wrap_text`가 공백에서만 자르는데 일본어 제목엔 대시 옆 공백 하나뿐이라 그렇다. 자막과
  **공유하는 기존 동작**이라 이번엔 안 건드렸다
- **첫 씬이 짧으면 제목이 번쩍인다.** 줄 길이 하한이 0.8초다. 실편에서 1.5초 미만이 나오면
  ADR-0065 되돌릴 조건 1 (노출 하한을 계약에 넣는다)
- 상단 밴드 0.14가 쇼츠 UI에 가리는지는 **폰에서 봐야 안다** (되돌릴 조건 2)
- 기존 석빙고 run의 `scenes.timed.*.json`엔 `title`이 없다 — `[3]` 재실행 때 실린다

### 그대로 남은 대기

- **(39)·(41)의 `[3]` 재실행**: `python run.py tts seokbinggo --force` → `[9] assemble`.
  이번 폰트(41)와 제목(42)이 실편에 들어가는 것은 그때다
- (38)·(39) 사람 판독: 폰 자막 크기 · 0.4MP 화질, 씬 7·12 재사용, tesseract PATH 미설치
- (41)이 남긴 것: Do Hyeon에 `·`(U+00B7) 없음 · `[8]` 크레딧 폰트가 알파벳순 첫 파일 ·
  `LINE_HEIGHT_RATIO` 1.2 vs 실측 1.0
- (40)이 남긴 질문: 구조 자유화의 안전망이 지금은 사람 판독 하나다
- (40)의 나머지: `read_status()` 호출자 0개 · atempo 상한 1.2가 schema에 없음 ·
  Fable 5 한도 메시지가 재시도 패턴을 비껴감 · `contract_pisa.json` 25씬 · `[10] mix` 미구현
